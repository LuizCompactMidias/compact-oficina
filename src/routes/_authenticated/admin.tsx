import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useEffect, useMemo, useState } from "react";
import { AlertTriangle, BadgeDollarSign, BarChart3, CalendarDays, Car, ChevronRight, CircleDollarSign, ClipboardList, FileInput, Gauge, LayoutDashboard, LogOut, Menu, PackageSearch, ReceiptText, Settings, UserCog, Users, WalletCards, Wrench, X } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { BRAND } from "@/lib/brand";
import { money, STATUS_LABEL, type OrderStatus } from "@/lib/porfirid-queries";
import { CustomersManager } from "@/components/admin/CustomersManager";
import { VehiclesManager } from "@/components/admin/VehiclesManager";
import { NewWorkOrderFlow } from "@/components/admin/NewWorkOrderFlow";
import { WorkOrderDetail } from "@/components/admin/WorkOrderDetail";
import { StockManager } from "@/components/admin/StockManager";
import { AgendaManager } from "@/components/admin/AgendaManager";
import { QuotesManager } from "@/components/admin/QuotesManager";
import { ServiceCatalogManager } from "@/components/admin/ServiceCatalogManager";
import { FinanceManager } from "@/components/admin/FinanceManager";
import { CommissionManager } from "@/components/admin/CommissionManager";
import { PurchaseInvoiceManager } from "@/components/admin/PurchaseInvoiceManager";
import { UserManagement } from "@/components/admin/UserManagement";
import { SettingsManager } from "@/components/admin/SettingsManager";

export const Route=createFileRoute("/_authenticated/admin")({head:()=>({meta:[{title:`Gestão | ${BRAND.name}`},{name:"description",content:"Painel administrativo completo para oficinas e centros automotivos."}]}),component:AdminPage});

type ModuleId="dashboard"|"orders"|"quotes"|"customers"|"vehicles"|"services"|"stock"|"purchases"|"agenda"|"finance"|"commissions"|"reports"|"users"|"settings";
type NavItem={id:ModuleId;label:string;icon:any};
const navItems:NavItem[]=[
{id:"dashboard",label:"Visão geral",icon:LayoutDashboard},{id:"orders",label:"Ordens de serviço",icon:ClipboardList},{id:"quotes",label:"Orçamentos",icon:ReceiptText},{id:"customers",label:"Clientes",icon:Users},{id:"vehicles",label:"Veículos",icon:Car},{id:"services",label:"Catálogo de serviços",icon:Wrench},{id:"stock",label:"Estoque e peças",icon:PackageSearch},{id:"purchases",label:"Entrada de Nota Fiscal",icon:FileInput},{id:"agenda",label:"Agenda",icon:CalendarDays},{id:"finance",label:"Financeiro",icon:WalletCards},{id:"commissions",label:"Comissões",icon:BadgeDollarSign},{id:"reports",label:"Relatórios",icon:BarChart3},{id:"users",label:"Usuários e acessos",icon:UserCog},{id:"settings",label:"Configurações",icon:Settings},
];
const roleModules:Record<string,ModuleId[]>={admin:navItems.map(i=>i.id),atendimento:["dashboard","orders","quotes","customers","vehicles","services","agenda"],tecnico:["dashboard","orders","vehicles","services","stock"],financeiro:["dashboard","purchases","finance","commissions","reports"]};

function AdminPage(){
 const [active,setActive]=useState<ModuleId>("dashboard"),[mobile,setMobile]=useState(false),[email,setEmail]=useState("Equipe COMPACT"),[role,setRole]=useState("admin"),[openOrder,setOpenOrder]=useState<string|null>(null);const navigate=useNavigate();
 useEffect(()=>{void(async()=>{const {data}=await supabase.auth.getUser();if(data.user?.email)setEmail(data.user.email);if(data.user){const {data:p}=await supabase.from("profiles").select("role,active").eq("id",data.user.id).maybeSingle();if(p?.active===false){await supabase.auth.signOut();navigate({to:"/login",replace:true});return}if(p?.role)setRole(p.role)}})()},[navigate]);
 const visible=useMemo(()=>navItems.filter(i=>(roleModules[role]??[]).includes(i.id)),[role]);
 useEffect(()=>{const fallback=visible[0];if(fallback&&!visible.some(i=>i.id===active))setActive(fallback.id)},[active,visible]);
 async function signOut(){await supabase.auth.signOut();navigate({to:"/login",replace:true})}
 function select(id:ModuleId){setActive(id);setMobile(false);setOpenOrder(null)}
 const current=visible.find(i=>i.id===active)??visible[0]??navItems[0];
 return <div className="min-h-screen bg-[#f4f5f7] text-[#17181A]"><aside className="fixed inset-y-0 left-0 z-40 hidden w-72 flex-col border-r border-white/8 bg-[#09090a] text-white lg:flex"><Brand/><Navigation items={visible} active={active} select={select}/><UserPanel email={email} role={role} signOut={signOut}/></aside>{mobile&&<div className="fixed inset-0 z-50 lg:hidden"><button className="absolute inset-0 bg-black/60" aria-label="Fechar menu" onClick={()=>setMobile(false)}/><aside className="relative flex h-full w-[88%] max-w-80 flex-col bg-[#09090a] text-white"><div className="flex items-center justify-between"><Brand/><button className="mr-4 rounded-xl border border-white/10 p-2" onClick={()=>setMobile(false)}><X className="size-5"/></button></div><Navigation items={visible} active={active} select={select}/><UserPanel email={email} role={role} signOut={signOut}/></aside></div>}<main className="lg:pl-72"><header className="sticky top-0 z-30 border-b border-black/6 bg-white/90 backdrop-blur-xl"><div className="flex h-20 items-center gap-4 px-4 sm:px-6 lg:px-8"><button onClick={()=>setMobile(true)} className="rounded-xl border border-black/10 p-2 lg:hidden"><Menu className="size-5"/></button><div className="min-w-0 flex-1"><p className="text-xs font-black uppercase tracking-[.2em] text-[#B97900]">Gestão da oficina</p><h1 className="truncate text-2xl font-black">{openOrder?"Detalhes da Ordem de Serviço":current?.label??"Painel"}</h1></div><button onClick={()=>void signOut()} className="inline-flex items-center gap-2 rounded-xl border border-black/10 px-3 py-2 text-sm font-bold"><LogOut className="size-4"/><span className="hidden sm:inline">Sair</span></button></div></header><section className="p-4 sm:p-6 lg:p-8">{openOrder?<WorkOrderDetail orderId={openOrder} onBack={()=>setOpenOrder(null)} userRole={role}/>:<ModuleSwitch active={active} role={role} openOrder={id=>{setActive("orders");setOpenOrder(id)}}/>}</section></main></div>
}
function Brand(){return <div className="flex h-20 items-center gap-3 px-5"><img src={BRAND.iconUrl} alt="" className="size-12 rounded-2xl object-contain"/><div><p className="text-lg font-black compact-gold-text">COMPACT</p><p className="text-[10px] font-bold uppercase tracking-[.18em] text-white/40">Centro Automotivo</p></div></div>}
function Navigation({items,active,select}:{items:NavItem[];active:ModuleId;select:(id:ModuleId)=>void}){return <nav className="flex-1 space-y-1 overflow-y-auto px-4 py-4">{items.map(item=>{const I=item.icon,on=active===item.id;return <button key={item.id} onClick={()=>select(item.id)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-bold transition ${on?"bg-[#F0B323] text-black":"text-white/62 hover:bg-white/7 hover:text-white"}`}><I className="size-5"/><span className="flex-1">{item.label}</span>{on&&<ChevronRight className="size-4"/>}</button>})}</nav>}
function UserPanel({email,role,signOut}:{email:string;role:string;signOut:()=>void}){const labels:Record<string,string>={admin:"Administrador",atendimento:"Atendimento",tecnico:"Técnico",financeiro:"Financeiro"};return <div className="border-t border-white/8 p-4"><div className="rounded-2xl bg-white/[.04] p-4"><p className="truncate text-sm font-bold">{email}</p><p className="mt-1 text-xs text-white/35">{labels[role]??role}</p><button onClick={()=>void signOut()} className="mt-3 flex w-full items-center justify-center gap-2 rounded-xl border border-white/10 py-2 text-xs font-bold text-white/60"><LogOut className="size-4"/>Sair</button></div></div>}
function ModuleSwitch({active,role,openOrder}:{active:ModuleId;role:string;openOrder:(id:string)=>void}){if(active==="dashboard")return <Dashboard openOrder={openOrder}/>;if(active==="orders")return <Orders openOrder={openOrder} userRole={role}/>;if(active==="quotes")return <QuotesManager onOpenOrder={openOrder}/>;if(active==="customers")return <CustomersManager userRole={role} onOpenOrder={openOrder}/>;if(active==="vehicles")return <VehiclesManager userRole={role} onOpenOrder={openOrder}/>;if(active==="services")return <ServiceCatalogManager/>;if(active==="stock")return <StockManager/>;if(active==="purchases")return <PurchaseInvoiceManager/>;if(active==="agenda")return <AgendaManager onOpenOrder={openOrder} userRole={role}/>;if(active==="finance")return <FinanceManager/>;if(active==="commissions")return <CommissionManager userRole={role}/>;if(active==="reports")return <Reports/>;if(active==="users")return <UserManagement/>;return <SettingsManager/>}

function Dashboard({openOrder}:{openOrder:(id:string)=>void}){
 const [metrics,setMetrics]=useState({open:0,customers:0,vehicles:0,revenue:0,ready:0,approval:0,averageTicket:0,lowStock:0,todayAgenda:0,pendingExpenses:0}),[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true);
 useEffect(()=>{void(async()=>{setLoading(true);const[o,c,v,i,a,e]=await Promise.all([
  supabase.from("work_orders").select("id,order_number,status,total,created_at,customers(name),vehicles(plate,brand,model)").order("created_at",{ascending:false}),
  supabase.from("customers").select("id",{count:"exact",head:true}),
  supabase.from("vehicles").select("id",{count:"exact",head:true}),
  supabase.from("inventory_items").select("id,quantity,min_quantity,active"),
  supabase.from("appointments").select("id,scheduled_at,status"),
  supabase.from("expenses").select("id,amount,status")
 ]);const r=(o.data??[]) as any[];const delivered=r.filter(x=>x.status==="entregue");const revenue=delivered.reduce((s,x)=>s+Number(x.total??0),0);const today=new Date().toDateString();const inventory=(i.data??[]) as any[],appointments=(a.data??[]) as any[],expenses=(e.data??[]) as any[];setRows(r.slice(0,8));setMetrics({open:r.filter(x=>!["entregue","cancelada"].includes(x.status)).length,customers:c.count??0,vehicles:v.count??0,revenue,ready:r.filter(x=>x.status==="pronto_entrega").length,approval:r.filter(x=>x.status==="aguardando_aprovacao").length,averageTicket:delivered.length?revenue/delivered.length:0,lowStock:inventory.filter(x=>x.active!==false&&Number(x.quantity)<=Number(x.min_quantity)).length,todayAgenda:appointments.filter(x=>!["cancelado","nao_compareceu"].includes(x.status)&&new Date(x.scheduled_at).toDateString()===today).length,pendingExpenses:expenses.filter(x=>x.status!=="pago"&&x.status!=="cancelado").reduce((s,x)=>s+Number(x.amount??0),0)});setLoading(false)})()},[]);
 return <div className="space-y-6"><div className="rounded-[2rem] bg-[#111214] p-7 text-white shadow-xl"><p className="text-xs font-black uppercase tracking-[.2em] text-[#F0B323]">Central operacional</p><h2 className="mt-2 text-3xl font-black">Visão geral da oficina</h2><p className="mt-2 text-sm text-white/45">Clientes, veículos, OS, agenda, estoque e financeiro integrados em tempo real.</p></div>{loading?<div className="h-32 animate-pulse rounded-3xl bg-black/5"/>:<><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="OS abertas" value={metrics.open} icon={ClipboardList}/><Metric label="Aguardando aprovação" value={metrics.approval} icon={ReceiptText}/><Metric label="Prontas para entrega" value={metrics.ready} icon={Car}/><Metric label="Agenda de hoje" value={metrics.todayAgenda} icon={CalendarDays}/><Metric label="Clientes" value={metrics.customers} icon={Users}/><Metric label="Veículos" value={metrics.vehicles} icon={Car}/><Metric label="Faturamento entregue" value={money(metrics.revenue)} icon={CircleDollarSign}/><Metric label="Ticket médio" value={money(metrics.averageTicket)} icon={Gauge}/></div><div className="grid gap-3 md:grid-cols-3"><Attention label="Itens com estoque baixo" value={String(metrics.lowStock)} danger={metrics.lowStock>0}/><Attention label="Despesas pendentes" value={money(metrics.pendingExpenses)} danger={metrics.pendingExpenses>0}/><Attention label="OS aguardando cliente" value={String(metrics.approval)} danger={metrics.approval>0}/></div></>}<section className="rounded-2xl border border-black/6 bg-white p-5 shadow-sm"><div className="flex items-end justify-between gap-3"><div><h3 className="font-black">Últimas ordens de serviço</h3><p className="mt-1 text-xs text-black/40">Acesse rapidamente os atendimentos mais recentes.</p></div><span className="text-xs font-bold text-black/35">{rows.length} exibidas</span></div><div className="mt-4 space-y-2">{rows.map(r=><button key={r.id} onClick={()=>openOrder(r.id)} className="flex w-full flex-col gap-2 rounded-xl border border-black/7 p-3 text-left hover:border-[#F0B323]/50 sm:flex-row sm:items-center"><strong className="text-[#8A5F00]">OS #{r.order_number}</strong><span className="flex-1 text-sm">{r.customers?.name??"Cliente"} • {r.vehicles?.brand??""} {r.vehicles?.model??""} • {r.vehicles?.plate??"—"}</span><Status value={r.status}/><strong>{money(Number(r.total??0))}</strong></button>)}{rows.length===0&&<p className="py-6 text-center text-sm text-black/40">Nenhuma OS registrada.</p>}</div></section></div>}
function Metric({label,value,icon:I}:{label:string;value:string|number;icon:any}){return <div className="rounded-2xl border border-black/6 bg-white p-5 shadow-sm"><div className="flex size-11 items-center justify-center rounded-xl bg-[#F0B323]/15 text-[#8A5F00]"><I className="size-5"/></div><p className="mt-5 text-xs font-bold uppercase tracking-wider text-black/35">{label}</p><p className="mt-1 text-2xl font-black">{value}</p></div>}
function Attention({label,value,danger}:{label:string;value:string;danger:boolean}){return <div className={`flex items-center gap-3 rounded-2xl border p-4 ${danger?"border-amber-200 bg-amber-50":"border-black/6 bg-white"}`}><div className={`flex size-10 items-center justify-center rounded-xl ${danger?"bg-amber-100 text-amber-700":"bg-black/5 text-black/35"}`}><AlertTriangle className="size-5"/></div><div><p className="text-xs font-bold text-black/45">{label}</p><p className="mt-1 text-lg font-black">{value}</p></div></div>}
function Status({value}:{value:string}){return <span className="rounded-full bg-[#F0B323]/15 px-2.5 py-1 text-[10px] font-black uppercase text-[#7A5400]">{STATUS_LABEL[value as OrderStatus]??value}</span>}
function Orders({openOrder,userRole}:{openOrder:(id:string)=>void;userRole:string}){const [rows,setRows]=useState<any[]>([]),[showNew,setShowNew]=useState(false),[search,setSearch]=useState("");const canCreate=["admin","atendimento"].includes(userRole);async function load(){const {data}=await supabase.from("work_orders").select("id,order_number,status,total,entry_at,customers(name),vehicles(plate,brand,model)").order("created_at",{ascending:false});setRows((data??[]) as any[])}useEffect(()=>{void load()},[]);const filtered=useMemo(()=>rows.filter(r=>!search.trim()||String(r.order_number).includes(search)||String(r.customers?.name??"").toLowerCase().includes(search.toLowerCase())||String(r.vehicles?.plate??"").toLowerCase().includes(search.toLowerCase())),[rows,search]);if(showNew&&canCreate)return <div className="space-y-4"><button onClick={()=>setShowNew(false)} className="secondaryButton">← Voltar para as OS</button><NewWorkOrderFlow onCreated={result=>{setShowNew(false);void load();openOrder(result.id)}}/></div>;return <div className="space-y-5"><div className="flex flex-col gap-3 rounded-2xl border border-black/6 bg-white p-5 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-black">Ordens de serviço</h2><p className="mt-1 text-sm text-black/45">Da recepção até a entrega do veículo.</p>{!canCreate&&<p className="mt-2 text-xs font-bold text-[#8A5F00]">Modo técnico: consulte e execute OS já abertas pelo atendimento.</p>}</div>{canCreate&&<button onClick={()=>setShowNew(true)} className="goldButton">Nova OS completa</button>}</div><input className="field max-w-xl" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar por OS, cliente ou placa..."/><div className="grid gap-3">{filtered.map(r=><button key={r.id} onClick={()=>openOrder(r.id)} className="rounded-2xl border border-black/6 bg-white p-5 text-left shadow-sm hover:border-[#F0B323]/50"><div className="flex flex-col gap-3 sm:flex-row sm:items-center"><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><strong className="text-lg text-[#8A5F00]">OS #{r.order_number}</strong><Status value={r.status}/></div><p className="mt-2 font-bold">{r.customers?.name??"Cliente"}</p><p className="mt-1 text-xs text-black/45">{r.vehicles?`${r.vehicles.brand} ${r.vehicles.model} • ${r.vehicles.plate}`:"Veículo"}</p></div><strong className="text-xl">{money(Number(r.total??0))}</strong></div></button>)}{filtered.length===0&&<div className="rounded-2xl border border-dashed border-black/10 bg-white p-10 text-center text-sm text-black/40">Nenhuma OS encontrada.</div>}</div></div>}
function Reports(){
 const [rows,setRows]=useState<any[]>([]),[expenses,setExpenses]=useState<any[]>([]),[start,setStart]=useState(""),[end,setEnd]=useState("");
 useEffect(()=>{void Promise.all([supabase.from("work_orders").select("id,order_number,status,payment_status,subtotal_services,subtotal_parts,discount,total,entry_at,customers(name),vehicles(plate)").order("entry_at",{ascending:false}),supabase.from("expenses").select("id,description,amount,status,due_date,created_at").order("created_at",{ascending:false})]).then(([o,e])=>{setRows((o.data??[]) as any[]);setExpenses((e.data??[]) as any[])})},[]);
 const filtered=useMemo(()=>rows.filter(r=>{const d=new Date(r.entry_at);if(start&&d<new Date(`${start}T00:00:00`))return false;if(end&&d>new Date(`${end}T23:59:59`))return false;return true}),[rows,start,end]);
 const filteredExpenses=useMemo(()=>expenses.filter(r=>{const d=new Date(r.due_date||r.created_at);if(start&&d<new Date(`${start}T00:00:00`))return false;if(end&&d>new Date(`${end}T23:59:59`))return false;return true}),[expenses,start,end]);
 const delivered=filtered.filter(r=>r.status==="entregue"),revenue=delivered.reduce((s,r)=>s+Number(r.total??0),0),services=filtered.reduce((s,r)=>s+Number(r.subtotal_services??0),0),parts=filtered.reduce((s,r)=>s+Number(r.subtotal_parts??0),0),discounts=filtered.reduce((s,r)=>s+Number(r.discount??0),0),paidExpenses=filteredExpenses.filter(r=>r.status==="pago").reduce((s,r)=>s+Number(r.amount??0),0),pendingExpenses=filteredExpenses.filter(r=>r.status!=="pago"&&r.status!=="cancelado").reduce((s,r)=>s+Number(r.amount??0),0),result=revenue-paidExpenses;
 return <div className="space-y-5"><div className="rounded-2xl border border-black/6 bg-white p-5 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><h2 className="text-xl font-black">Relatórios operacionais e financeiros</h2><p className="mt-1 text-sm text-black/45">Analise OS, serviços, peças, faturamento, despesas e resultado por período.</p></div><div className="flex flex-wrap gap-2"><label className="text-xs font-bold text-black/45">De<input type="date" className="field mt-1 py-2" value={start} onChange={e=>setStart(e.target.value)}/></label><label className="text-xs font-bold text-black/45">Até<input type="date" className="field mt-1 py-2" value={end} onChange={e=>setEnd(e.target.value)}/></label>{(start||end)&&<button onClick={()=>{setStart("");setEnd("")}} className="secondaryButton self-end">Limpar período</button>}</div></div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="OS no período" value={filtered.length} icon={ClipboardList}/><Metric label="Faturamento entregue" value={money(revenue)} icon={CircleDollarSign}/><Metric label="Ticket médio entregue" value={money(delivered.length?revenue/delivered.length:0)} icon={Gauge}/><Metric label="Resultado operacional" value={money(result)} icon={BarChart3}/><Metric label="Mão de obra lançada" value={money(services)} icon={Wrench}/><Metric label="Peças lançadas" value={money(parts)} icon={PackageSearch}/><Metric label="Descontos concedidos" value={money(discounts)} icon={ReceiptText}/><Metric label="Despesas pendentes" value={money(pendingExpenses)} icon={WalletCards}/></div><div className="overflow-x-auto rounded-2xl border border-black/6 bg-white"><table className="w-full min-w-[980px] text-left text-sm"><thead className="bg-black/[.025] text-xs uppercase text-black/40"><tr><th className="px-4 py-3">OS</th><th>Data</th><th>Cliente</th><th>Placa</th><th>Status</th><th>Pagamento</th><th>Serviços</th><th>Peças</th><th>Desconto</th><th>Total</th></tr></thead><tbody>{filtered.map(r=><tr key={r.id} className="border-t border-black/6"><td className="px-4 py-3 font-black text-[#8A5F00]">#{r.order_number}</td><td>{r.entry_at?new Date(r.entry_at).toLocaleDateString("pt-BR"):"—"}</td><td>{r.customers?.name??"—"}</td><td>{r.vehicles?.plate??"—"}</td><td><Status value={r.status}/></td><td><span className="text-xs font-bold uppercase text-black/55">{String(r.payment_status??"pendente").replaceAll("_"," ")}</span></td><td>{money(r.subtotal_services)}</td><td>{money(r.subtotal_parts)}</td><td>{money(r.discount)}</td><td className="font-black">{money(r.total)}</td></tr>)}{filtered.length===0&&<tr><td colSpan={10} className="px-4 py-10 text-center text-black/40">Nenhuma OS no período selecionado.</td></tr>}</tbody></table></div><div className="rounded-2xl border border-black/6 bg-white p-5"><h3 className="font-black">Resumo de despesas</h3><div className="mt-4 grid gap-3 sm:grid-cols-3"><MiniReport label="Despesas no período" value={money(filteredExpenses.reduce((s,r)=>s+Number(r.amount??0),0))}/><MiniReport label="Pagas" value={money(paidExpenses)}/><MiniReport label="Pendentes" value={money(pendingExpenses)}/></div></div></div>}
function MiniReport({label,value}:{label:string;value:string}){return <div className="rounded-xl bg-black/[.03] p-4"><p className="text-xs font-bold text-black/40">{label}</p><p className="mt-1 text-xl font-black">{value}</p></div>}
