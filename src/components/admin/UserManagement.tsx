import { useEffect, useState } from "react";
import { ShieldCheck, UserCog } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
const roles=[{value:"admin",label:"Administrador"},{value:"atendimento",label:"Atendimento"},{value:"tecnico",label:"Técnico"},{value:"financeiro",label:"Financeiro"}];
export function UserManagement(){const [rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true),[message,setMessage]=useState("");async function load(){setLoading(true);const {data,error}=await supabase.from("profiles").select("id,full_name,role,active,created_at").order("created_at");if(!error)setRows(data??[]);setLoading(false)}useEffect(()=>{void load()},[]);async function change(id:string,patch:any){setMessage("");const {error}=await supabase.from("profiles").update({...patch,updated_at:new Date().toISOString()}).eq("id",id);if(error){setMessage(error.message);return}setMessage("Acesso atualizado.");await load()}return <div className="space-y-5"><div className="rounded-2xl border border-black/6 bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-xl bg-[#F0B323]/15 text-[#8A5F00]"><UserCog className="size-5"/></div><div><h2 className="text-xl font-black">Usuários e acessos</h2><p className="mt-1 text-sm text-black/45">Defina função e ative ou bloqueie o acesso de cada usuário autenticado.</p></div></div></div>{message&&<p className="rounded-xl bg-[#F0B323]/12 px-4 py-3 text-sm font-bold text-[#6F4B00]">{message}</p>}{loading?<p>Carregando...</p>:<div className="grid gap-3">{rows.map(u=><div key={u.id} className="rounded-2xl border border-black/6 bg-white p-5 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-center"><div className="flex-1"><p className="font-black">{u.full_name||"Usuário sem nome"}</p><p className="mt-1 text-xs text-black/40">ID {u.id}</p></div><label className="text-xs font-bold">Função<select value={u.role} onChange={e=>void change(u.id,{role:e.target.value})} className="field mt-1 min-w-44">{roles.map(r=><option key={r.value} value={r.value}>{r.label}</option>)}</select></label><button onClick={()=>void change(u.id,{active:!u.active})} className={`rounded-xl px-4 py-3 text-sm font-black ${u.active?"bg-emerald-50 text-emerald-700":"bg-red-50 text-red-700"}`}><ShieldCheck className="mr-2 inline size-4"/>{u.active?"Ativo":"Bloqueado"}</button></div></div>)}</div>}<div className="rounded-2xl border border-dashed border-black/10 bg-white p-4 text-sm text-black/50">Novos usuários são criados no Authentication do Supabase. Depois, a função e o bloqueio podem ser administrados por esta tela.</div></div>}
